name = input("Name: ")
age = input("Age: ")
course = input("Course: ")
dream_job = input("Dream Job: ")
favorite_language = input("Favorite programming language: ")


print(f"Welcome {name}!\nLet's create your student profile\n====STUDENT PROFILE===\nName: {name}\nAge: {age}\nCourse: {course}\nDream Job: {dream_job}\nFavorite programming language: {favorite_language}")




"""
# Day 2 - User Input and Output Formatting

## Topics Learned

* `input()`
* Variables
* `print()`
* f-strings
* New line character (`\n`)
* Quotes (`'` and `"`)
* Escape characters (`\'`, `\\`)

## Key Concepts

### User Input

The `input()` function allows users to enter information from the keyboard.

### Variables

Variables store data that can be reused throughout a program.

### f-Strings

f-strings make it easy to insert variables into text output.

### New Line Character

`\n` moves the output to the next line.

### Quotes

Python supports both single quotes (`'`) and double quotes (`"`).

### Escape Characters

Escape characters allow special characters to be used inside strings.

Examples:

* `\n` = New Line
* `\'` = Single Quote
* `\\` = Backslash

## Project Completed

### Student Profile Generator

The program:

1. Asks the user for:

   * Name
   * Age
   * Course
   * Dream Job
   * Favorite Programming Language

2. Displays:

   * Welcome Message
   * Student Profile Summary

## Challenges Encountered

### Apostrophe Error

Problem:
Using an apostrophe inside a string enclosed by single quotes caused a syntax error.

Example:

* `Let's`

Solution:

* Use double quotes for the string.
* Or escape the apostrophe using `\'`.

## What I Learned

* How to collect information from users.
* How to store input in variables.
* How to format output using f-strings.
* How to create multi-line output with `\n`.
* How escape characters work in Python strings.

## Reflection

Today I created my first interactive Python program. I learned how Python receives user input and how to display information in a clean and organized format.

## Next Topic

Day 3 - Data Types and Type Conversion

* `str`
* `int`
* `float`
* `type()`
* Converting strings into numbers
* Basic calculations
# Day 2 - User Input and Output Formatting

## Topics Learned

* `input()`
* Variables
* `print()`
* f-strings
* New line character (`\n`)
* Quotes (`'` and `"`)
* Escape characters (`\'`, `\\`)

## Key Concepts

### User Input

The `input()` function allows users to enter information from the keyboard.

### Variables

Variables store data that can be reused throughout a program.

### f-Strings

f-strings make it easy to insert variables into text output.

### New Line Character

`\n` moves the output to the next line.

### Quotes

Python supports both single quotes (`'`) and double quotes (`"`).

### Escape Characters

Escape characters allow special characters to be used inside strings.

Examples:

* `\n` = New Line
* `\'` = Single Quote
* `\\` = Backslash

## Project Completed

### Student Profile Generator

The program:

1. Asks the user for:

   * Name
   * Age
   * Course
   * Dream Job
   * Favorite Programming Language

2. Displays:

   * Welcome Message
   * Student Profile Summary

## Challenges Encountered

### Apostrophe Error

Problem:
Using an apostrophe inside a string enclosed by single quotes caused a syntax error.

Example:

* `Let's`

Solution:

* Use double quotes for the string.
* Or escape the apostrophe using `\'`.

## What I Learned

* How to collect information from users.
* How to store input in variables.
* How to format output using f-strings.
* How to create multi-line output with `\n`.
* How escape characters work in Python strings.

## Reflection

Today I created my first interactive Python program. I learned how Python receives user input and how to display information in a clean and organized format.

## Next Topic


"""